let index = 0;
let width = 20;

while (index < width) {
    repeat(width - index, func() {
        dump(" ");
    });
    repeat(index, func() {
        dump(" *");
    });
    print("");
    index = index + 1;
};
