func fib(n) {
  let last = 0;
  let curr = 1;

  let tmp;
  n.times(func() {
    tmp = curr;
    curr = last + curr;
    last = tmp;
  });

  last;
};

let n = "Which fibonacci number do you want?: ".prompt().to_n();
print(fib(n));
