# Calculate the nth fibonacci number
func fib(n) {
    let last = 0;
    let curr = 1;

    let i = 0;
    while (i < n - 1) {
        i = i + 1;
        let tmp = curr;
        curr = last + curr;
        last = tmp;
    };
    curr;
};

print(fib(Number(gets())));
