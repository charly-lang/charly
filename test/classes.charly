class Person {
    let name;
    let age;

    func constructor(_name, _age) {
        name = _name;
        age = _age;
    };
};

let leonard = new(Person, "Leonard", 16);
let peter = new(Person, "Peter", 20);

print(memberex(leonard, "name"));
print(memberex(leonard, "age"));
print(memberex(peter, "name"));
print(memberex(peter, "age"));
