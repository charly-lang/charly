let Numeric = {
  func times(callback) {
    repeat(self, func(i) {
      callback(i, self.to_string());
    });
  };

  func to_string() {
    "Value: " + self;
  };
};

func call_me() {
  5.times(func(i, value) {
    print(i, value);
  });
};

call_me();
