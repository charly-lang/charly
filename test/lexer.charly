func return_test(amount) {
  let index = 0;
  while (index < amount) {
    index = index + 1;
  };

  if (index > 0) {
    index;
  } else {
    0;
  };
};

let sum = return_test(-1-000); # Should be 1000
