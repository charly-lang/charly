let ar = [1, 2];
let st = "test";

let a = member_read(ar, 0); # 1
let b = member_read(st, 0); # "t"

let c = member_write(ar, 0, 5); # 5
let d = member_write(st, 0, "f"); # "f"

let e = member_insert(ar, 0, 1); # 1
let f = member_insert(st, 0, "october"); # "october"
let tmp = st;

let g = member_delete(ar, 0); # 1
let h = member_delete(st, 1); # c
