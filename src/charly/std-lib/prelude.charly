func include(filename) {
  call_internal("include", filename);
};

func require(filename) {
  call_internal("require", filename);
};

func length(arg) {
  call_internal("length", arg);
};

# Default print and write functions
let io = require("io");
let print = io.stdout.print;
let write = io.stdout.write;
let gets = io.stdin.gets;
let getc = io.stdin.getc;
