class Exception {
  let message
  let code
  let payload

  func constructor(message) {
    self.message = message
    self.code = 0
    self.payload = null
  }

  func to_s() {
    self.message
  }
}

export = {
  const Exception = Exception
}
